{% extends 'base.html.twig' %}

{% block title %}Hello World!{% endblock %}

{% block body %}
  <main class="home-main">
    <section class="hero-section">
      <div class="hero-section-content">
        <h1>Bienvenue</h1>
        <div>
          <p>Découvrez notre univers de produits durables et éthiques pour une consommation responsable</p>
          <br />
          <p>Retrouvez des articles d&apos;hygiène & beauté, des accessoires & boissons et du prêt à porter</p>
        </div>
      </div>
      <div class="hero-section-image">
        <img src="{{ asset('images/welcome.jpg') }}" alt="Hero Image" />
      </div>
    </section>
    <section class="products-section">
      {% if products|length is same as(0) %}
        <p>Aucun produit trouvé</p>
      {% else %}
        <div class="products-section-title-container">
          <h2>Nos produits</h2>
        </div>
        <ul class="products-section-list">
          {% for product in products %}
            <li class="products-section-list-item">
              {% if product.picturePath %}
                <img class="products-section-list-item-image" src="{{ asset(product.picturePath) }}" alt="{{ product.name }}" />
              {% else %}
                <img class="products-section-list-item-image" src="{{ asset('images/products/product-default.jpg') }}" alt="Product 1" />
              {% endif %}
              <h3>{{ product.name }}</h3>
              <p class="products-section-list-item-description">{{ product.shortDescription }}</p>
              <p class="products-section-list-item-price">{{ product.price }}€</p>
              <a class="products-section-list-item-link" href="{{ path('app_product_show', { id: product.id }) }}">
                <p>Voir le produit</p>
              </a>
            </li>
          {% endfor %}
        </ul>
      {% endif %}
    </section>
    <section class="about-section">

      <div class="convictions-container">
        <div class="convictions-container-content">
          <h2>Notre conviction</h2>
          <p>
            Chez GreenGoodies, nous croyons en un monde où le commerce peut être synonyme de bien-être pour la planète et ses habitants. 
            Notre mission est de vous proposer une sélection rigoureuse de produits qui allient qualité, durabilité et éthique
          </p>
        </div>
        <div class="convictions-container-image">
          <img src="{{ asset('images/connexion.webp') }}" alt="plante" />
        </div>
      </div>

      <div class="values-container">
        <div class="values-container-image">
          <img src="{{ asset('images/garbages-green.jpg') }}" alt="plante" />
        </div>
        <div class="values-container-content">
          <h2>Nos valeurs</h2>
          <p>
            Durabilité : Nous sélectionnons soigneusement des produits qui respectent l'environnement et favorisent la durabilité.
            Éthique : Nous nous engageons à travailler avec des partenaires et des fournisseurs qui partagent nos valeurs éthiques.
            Écologie : Nous encourageons des pratiques respectueuses de l'écosystème et privilégions les matériaux écologiques.
          </p>
        </div>
      </div>

    </section>
  </main>
{% endblock %}
